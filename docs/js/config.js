// Set this to your Render backend URL after deploying (e.g. https://download-mp4.onrender.com).
// Leave empty to use same-origin (only works when frontend is served from the same host as the API).
window.API_BASE = '';
