# Port (default 3000)
PORT=3000

# Path to yt-dlp binary (optional; leave unset to use bundled/default)
# YT_DLP_PATH=/usr/local/bin/yt-dlp

# Temporary files directory (optional; defaults to ./tmp)
# TMP_DIR=./tmp
