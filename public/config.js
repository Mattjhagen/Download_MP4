// API base URL: leave empty when frontend and backend are same origin (e.g. localhost or single Render deploy).
// When using GitHub Pages, set this to your Render backend URL, e.g.:
//   window.API_BASE = 'https://download-mp4.onrender.com';
window.API_BASE = '';
